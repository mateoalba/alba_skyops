/**
 * Roles del sistema. Ajustar estos valores para que coincidan EXACTO
 * (mismo string) con lo que devuelve el backend de Django en el login/perfil.
 */
export const Role = {
  ADMIN: 'ADMIN',
  OPERADOR: 'OPERADOR',
  TRIPULANTE: 'TRIPULANTE',
};